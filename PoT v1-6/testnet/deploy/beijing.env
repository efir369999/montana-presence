# Time Testnet - Beijing Node Configuration
# Server: Beijing, China

# Network
POT_NETWORK=TESTNET
POT_NODE_NAME=beijing-01
POT_P2P_PORT=18333
POT_RPC_PORT=18332

# Bootstrap peers (Moscow node IP - REPLACE WITH ACTUAL IP)
# Format: ip:port,ip:port
POT_BOOTSTRAP_PEERS=MOSCOW_IP:18333

# Data directory
POT_DATA_DIR=/var/lib/pot

# Logging
POT_LOG_LEVEL=INFO

# Allow testnet features
POT_ALLOW_UNSAFE=1

# External IP (for peer discovery) - REPLACE WITH ACTUAL IP
POT_EXTERNAL_IP=BEIJING_IP
