{
  "name": "junona-montana",
  "version": "1.0.0",
  "description": "Юнона — Montana Protocol PWA",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build needed - remote URL'",
    "cap:init": "npx cap init Junona com.montana.junona --web-dir=www",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/contacts": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
