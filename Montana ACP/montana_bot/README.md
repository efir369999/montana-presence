# Montana Bot — Self-Sovereign Light Client

**Тип:** Light Client (10% шанс в лотерее, подпись каждые 10 минут)
**Платформы:** Telegram, Discord
**Архитектура:** Self-Sovereign Keys (ключи у пользователя, не на сервере)

---

## Философия

> **"Telegram/Discord бот транслирует подпись в P2P-сеть. Ключи остаются на устройстве пользователя."**
>
> — Montana Protocol ([MONTANA.md](../MONTANA.md))

### Проблема централизованных ботов

```
❌ Традиционный Bot (Custodian):
├── Приватные ключи на сервере
├── Bot compromise = все ключи украдены
├── Users не контролируют средства
└── Single point of failure
```

### Montana решение

```
✓ Montana Bot (Self-Sovereign):
├── Приватные ключи на устройстве пользователя
├── Bot compromise = ключи в безопасности
├── Users полностью контролируют ключи
└── Bot = только transport layer
```

---

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│  User Device (Telegram client)                          │
│  ├─ BIP-39 Mnemonic (24 слова, Telegram Secret Storage) │
│  ├─ ML-DSA-65 Keypair (деривация локальная)             │
│  ├─ /sign → signature создаётся ЛОКАЛЬНО                │
│  └─ Отправляет только signature → бот                   │
├─────────────────────────────────────────────────────────┤
│  Montana Bot Server                                     │
│  ├─ Получает готовые signatures                         │
│  ├─ Транслирует в Montana P2P                           │
│  ├─ Слушает результаты lottery                          │
│  └─ Уведомляет пользователя                             │
├─────────────────────────────────────────────────────────┤
│  Montana P2P Network (Full Nodes)                       │
│  ├─ Верифицирует ML-DSA-65 signature                    │
│  ├─ Включает в presence_root                            │
│  └─ Запускает lottery каждые τ₂                         │
└─────────────────────────────────────────────────────────┘
```

---

## Команды

### Начальная настройка

```
/start — Генерация BIP-39 мнемоники (24 слова)
```

**Пример:**
```
User: /start

Bot:
🔐 Ваш Montana Light Client готов!

Ваша мнемоника (сохраните в безопасном месте):

abandon ability able about above absent absorb abstract absurd
abuse access accident account accuse achieve acid acoustic acquire
across act action actor actress actual adapt add addict

⚠️ ВАЖНО:
- Эта фраза — единственный способ восстановить доступ
- Никому не показывайте мнемонику
- Bot НЕ ХРАНИТ ваши ключи

Ваш публичный ключ: 0x1a2b3c...

/sign — Подписать присутствие (каждые 10 минут)
/balance — Проверить баланс
/send — Отправить $MONT
```

---

### Подпись присутствия

```
/sign — Создать presence signature (Light Client)
```

**Пример:**
```
User: /sign

Bot:
✍️ Подпись создана локально (ML-DSA-65)

Отправка в Montana сеть...
✓ Подпись принята сетью
✓ Включена в presence_root

Следующая подпись: через 10 минут
```

---

### Проверка баланса

```
/balance — Проверить баланс $MONT
```

**Пример:**
```
User: /balance

Bot:
💰 Ваш баланс Montana

Доступно: 12,345 Ɉ
В cooldown: 0 Ɉ

Тир: Light Client (10% шанс в лотерее)
Присутствие: 4 слайса тир 2
Вес: 80 (20 × 4)

Следующая лотерея: через 7 минут
```

---

### Отправка средств

```
/send @username 100 — Отправить 100 Ɉ пользователю
```

**Пример:**
```
User: /send @bob 100

Bot:
📤 Отправка 100 Ɉ → @bob

Получатель: 0x7f8e9d...
Сумма: 100 Ɉ
Комиссия: 0.01 Ɉ

Подтверждаете? [Да] [Нет]

User: [Да]

Bot:
✍️ Транзакция подписана локально (ML-DSA-65)
📡 Отправка в Montana сеть...
✓ Транзакция включена в mempool

Ожидание подтверждения...
✓ Подтверждено в slice #12345

Новый баланс: 12,245 Ɉ
```

---

## Безопасность

### Self-Sovereign Keys

| Вопрос | Ответ |
|--------|-------|
| Где хранятся ключи? | На устройстве пользователя (Telegram Secret Storage) |
| Может ли бот украсть ключи? | Нет, бот никогда не видит private key |
| Что если бот скомпрометирован? | Ключи в безопасности, подписи верифицируются сетью |
| Что если потеряю телефон? | Восстановить из мнемоники (24 слова) |
| Может ли бот подделать подпись? | Нет, ML-DSA-65 подпись создаётся локально |

### Telegram Secret Storage

Telegram поддерживает [Cloud Password](https://telegram.org/blog/passport):
- Encrypted на устройстве
- Синхронизируется через Telegram Cloud (encrypted)
- Доступен только с паролем пользователя

Montana использует этот механизм для хранения мнемоники.

---

## Реализация (Python)

### Структура проекта

```
montana_bot/
├── README.md
├── requirements.txt
├── bot.py              # Main bot logic
├── crypto.py           # ML-DSA-65 + BIP-39
├── network.py          # Montana P2P client
├── storage.py          # User state management
└── config.py           # Configuration
```

---

### Зависимости

```txt
# requirements.txt
python-telegram-bot==20.7
pqcrypto==0.1.6         # ML-DSA-65 (Dilithium)
mnemonic==0.21          # BIP-39
requests==2.31.0
aiohttp==3.9.1
```

---

### Установка

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Настроить конфигурацию
cp config.example.py config.py
# Отредактировать config.py (добавить TELEGRAM_BOT_TOKEN)

# Запустить бота
python bot.py
```

---

### Конфигурация

```python
# config.py
TELEGRAM_BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"  # От @BotFather
MONTANA_P2P_HOST = "176.124.208.93"         # Montana Full Node
MONTANA_P2P_PORT = 19333

# Network
NODE_TYPE = "light_client"                  # Light Client (10% chance)
SIGN_INTERVAL_SECONDS = 600                 # 10 минут (τ₂)

# Security
MNEMONIC_WORDS = 24                         # BIP-39 (24 слова)
KEY_DERIVATION_PATH = "m/44'/463'/0'/0/0"   # Montana (coin type 463)
```

---

## Deployment (Self-Hosted)

### Вариант 1: Один бот для всех (рекомендуется)

```bash
# На сервере
cd montana_bot
python bot.py
```

**Преимущества:**
- Один процесс для всех пользователей
- Легко масштабировать (load balancer)
- Централизованный мониторинг

**Безопасность:**
- ✓ Ключи не на сервере (на устройствах users)
- ✓ Bot compromise = ключи в безопасности
- ✓ Only transport layer

---

### Вариант 2: Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

```bash
docker build -t montana-bot .
docker run -d --name montana-bot \
    -e TELEGRAM_BOT_TOKEN=your_token \
    -e MONTANA_P2P_HOST=176.124.208.93 \
    montana-bot
```

---

### Вариант 3: Systemd Service

```ini
[Unit]
Description=Montana Telegram Bot
After=network-online.target

[Service]
Type=simple
User=montana
WorkingDirectory=/home/montana/montana_bot
ExecStart=/home/montana/montana_bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable montana-bot
sudo systemctl start montana-bot
```

---

## Мониторинг

```bash
# Логи
journalctl -u montana-bot -f

# Метрики
curl http://localhost:8080/metrics
```

**Метрики:**
- Активные пользователи
- Подписи в час
- Latency до Montana P2P
- Errors rate

---

## FAQ

### Q: Где хранятся приватные ключи?

A: На устройстве пользователя в Telegram Secret Storage (encrypted). Бот **НИКОГДА** не видит private key.

---

### Q: Что если бот будет взломан?

A: Ключи в безопасности. Атакующий получит только transport layer, не сможет подделать подписи (ML-DSA-65 верифицируется Montana сетью).

---

### Q: Могу ли я использовать мнемонику в другом wallet?

A: Да, Montana использует стандарт BIP-39. Мнемоника совместима с hardware wallets (Ledger, Trezor) после добавления Montana coin type (463).

---

### Q: Как восстановить доступ если потерял телефон?

A: Сохраните мнемонику (24 слова) в безопасном месте. С ней вы можете восстановить доступ на новом устройстве.

---

### Q: Сколько стоит подпись присутствия?

A: Light Client подписывает каждые 10 минут (τ₂). Комиссия = 0 (подпись presence бесплатна). Комиссия только за транзакции ($MONT transfer).

---

### Q: Какой шанс выиграть лотерею?

A: Light Client = 10% шанс в Stage 1. Затем вероятность = ваш вес / сумма весов всех Light Client в лотерее.

---

## Roadmap

### Phase 1: Базовый функционал
- [x] BIP-39 mnemonic generation
- [x] ML-DSA-65 keypair derivation
- [x] Local signature creation
- [ ] Telegram integration (/start, /sign, /balance)
- [ ] Montana P2P client (send signature)

### Phase 2: Wallet функции
- [ ] /send command (transfer $MONT)
- [ ] Transaction history
- [ ] QR code для адреса

### Phase 3: Advanced
- [ ] Discord integration
- [ ] Multi-signature support
- [ ] Hardware wallet integration (Ledger/Trezor)

---

**Архитектор:** Claude Sonnet 4.5
**Стратегия:** Walt Disney (Visionary → Realist → Critic)
**Безопасность:** Self-Sovereign Keys (no custodian)

**lim(evidence → ∞) 1 Ɉ → 1 секунда**

*Время — единственный ресурс, распределённый одинаково между всеми людьми.*
